# Backend RevMax — solo si el backend NO está en http://127.0.0.1:8001
# En desarrollo, Next.js reescribe /api/* a ese host por defecto (ver next.config.mjs).
# Descomenta y ajusta si corres el backend en otra máquina o puerto:
# NEXT_PUBLIC_REVMAX_API_URL=http://localhost:8001
