# Backend RevMax (solo si NO usas el proxy de Vite).
# Con npm run dev el proxy envía /api a http://localhost:8001.
# Si sirves el frontend en otro puerto o sin proxy, descomenta:
# VITE_API_URL=http://localhost:8001
